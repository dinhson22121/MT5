# EA SETTINGS CHO $200 STANDARD ACCOUNT
# ⚠️ HIGH RISK - Chỉ dùng nếu OK với việc mất hết

# ==> COPY VÀO STRATEGY TESTER HOẶC LIVE CHART

## RISK MANAGEMENT (CRITICAL!)
InpPositionSizePercent = 0.5
# 0.5% of $200 = $1 per trade
# Với SL 30 pips Gold = ~$3 risk
# Max loss mỗi lệnh: ~1.5%

InpStopLossPips = 30
# Tighter SL để giảm risk per trade
InpTakeProfitPips = 90
# R:R = 1:3 (cần win 1 lệnh cover 3 lệnh loss)

InpBreakevenPips = 20
# Move to BE sớm

InpUseAutoMaxPositions = false
InpMaxPositions = 2
# CHỈ 2 lệnh cùng lúc
# Max risk = $6 (~3%)

## TRAILING STOP
InpUseTrailingStop = true
InpTrailingActivatePips = 30
InpTrailingDistancePips = 20
# Bảo vệ profit tốt hơn

## MODES (Conservative)
InpAllowTrendingBuy = true
InpAllowTrendingSell = true
InpAllowMomentumTrade = false
# TẮT Momentum (aggressive)
InpAllowSidewayTrade = false
# TẮT Sideways (unpredictable)

## FILTERS (Strict)
InpMaxSpreadPips = 3
# Chỉ trade khi spread <= 3 pips
InpVolumeMultiplier = 1.5
# Cần volume cao hơn
InpMinATRMultiplier = 1.0
# Cần volatility ổn định

InpUseRSIConfirmation = true
InpUseCandleConfirmation = true
# Bật hết confirmations

InpMinutesBetwenTrades = 180
# 3 giờ giữa các lệnh
# Tránh overtrading

## SESSIONS
InpUseTimeFilter = true
InpTradeAsianSession = false
# TẮT Asian (Gold ít predictable)
InpTradeEuropeanSession = true
# Chỉ trade EU session
InpTradeUSSession = false
# TẮT US (volatile)

## INDICATORS
InpMinADX = 25
# Chỉ trade khi trend rất mạnh

InpRSIPeriod = 14
InpRSIOversold = 30
InpRSIOverbought = 70

## SYMBOL: EURUSD ONLY
# Không trade Gold, BTC, Indices
# Chỉ EURUSD vì:
# - Spread thấp (0.5 pips)
# - Predictable
# - Less volatile
